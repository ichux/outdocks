version: '3.7'

networks:
  proxynet:
    name: idev
    external: true

services:
  dbmysql:
    image: mysql:8.0.20
    restart: always
    container_name: mysqlv8.0.20
    command: --innodb-dedicated-server=ON
    environment:
      MYSQL_DATABASE: docker
      MYSQL_USER: backend
      MYSQL_PASSWORD: 5ec8b1681
      MYSQL_ROOT_PASSWORD: e81bc8e51
    ports:
      - 3306:3306
    volumes:
      - $PWD/keeps/mysql:/var/lib/mysql
    networks:
      - proxynet
